#searchform
    button
        background-color var(--wwii-taust3)
        cursor pointer
    button:hover
        background-color var(--wwii-taust2)
#tulemused
    margin-top 2em

.search-result-name
    font-size 1.5em
    font-weight bold
    margin 0
    padding 0
    background-color var(--wwii-taust2)
    text-decoration none